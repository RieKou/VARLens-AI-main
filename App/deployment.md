# Deployment

Deploy `frontend` to Vercel with `NEXT_PUBLIC_API_URL` pointing to the Render API URL. Deploy `backend` as a Render Docker web service and set `DATABASE_URL`, `API_CORS_ORIGINS`, and the secrets in `.env.example`. Apply `database.sql` in Supabase for the production data model.
